## 설치 및 실행
```bash
# Expo 패키지 설치
   npx expo install expo
# 개발 서버 실행
   npx expo start
   npx expo start --web # 웹 브라우저에서 실행
   npx expo start --android # Android 에뮬레이터 실행
   npx expo start --ios # iOS 시뮬레이터 실행
```
